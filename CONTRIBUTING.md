# 项目贡献与维护说明

感谢你参与北京服装学院计算机知识学习社官方网站项目的维护！

本文档用于说明项目的基本开发规范。

如果你只是浏览、使用项目，可以直接阅读 [README.md](README.md)。

如果你准备修改代码、增加功能或提交 Pull Request，请阅读本文档。

---

## 一、基本原则

项目采用 GitHub 协作开发方式。

请遵循以下原则：

1. **不要直接修改 `main` 分支**
2. **一个分支尽量对应一个独立任务**
3. **新建分支前，请确保基于最新的 `main`**
4. **完成修改并自测后，通过 Pull Request 合并**
5. **不要提交密钥、密码、`.env` 等敏感信息**

`main` 是项目的主分支，应始终保持可以正常构建和运行。

---

## 二、分支规范

开发新功能或修改项目时，请创建自己的开发分支。

常用分支类型：

```text
feature/short-desc
fix/short-desc
refactor/short-desc
docs/short-desc
```

分别用于：

| 类型       | 用途               | 示例                    |
| ---------- | ------------------ | ----------------------- |
| `feature`  | 新功能、新页面     | `feature/activity-page` |
| `fix`      | Bug 修复           | `fix/mobile-layout`     |
| `refactor` | 代码重构           | `refactor/form-logic`   |
| `docs`     | 文档、README、注释 | `docs/update-readme`    |

例如：

```bash
git checkout main
git pull origin main
git checkout -b feature/activity-page
```

### 分支原则

一个分支尽量只负责一个独立任务。

例如：

```text
feature/activity-page
```

主要负责活动页面开发。

不要在同一个分支里同时进行：

```text
活动页面开发
+ 首页重构
+ 修复登录 Bug
+ 修改 README
```

这样会增加代码 Review 和后续维护的难度。

---

## 三、Commit 提交规范

Commit 信息建议采用以下格式：

```text
类型: 简短描述
```

常用类型：

```text
feat: 新增功能 / 页面
fix: 修复 Bug
docs: 修改文档、文案、注释
refactor: 代码重构
style: 样式、格式调整
```

例如：

```bash
git commit -m "feat: 新增社团活动页面"
```

或者：

```bash
git commit -m "fix: 修复移动端页面显示问题"
```

```bash
git commit -m "docs: 更新项目维护说明"
```

Commit 描述尽量简单明确，让其他人能够快速理解这次提交做了什么。

---

## 四、Pull Request

完成开发后，将自己的开发分支提交 Pull Request。

PR 的目标分支统一为：

```text
main
```

基本流程：

```text
开发
 ↓
自测
 ↓
commit
 ↓
push
 ↓
创建 Pull Request
 ↓
Review
 ↓
修改（如有需要）
 ↓
合并
```

### 提交 PR 前

请至少确认：

- [ ] 本地项目可以正常启动
- [ ] 修改的功能可以正常运行
- [ ] 没有明显的控制台报错
- [ ] 没有提交 API Key、Token、密码等敏感信息
- [ ] 没有提交不必要的构建产物
- [ ] 页面修改已经进行基本测试

---

## 五、代码 Review

提交 PR 后，由其他维护者进行 Review。

Review 主要关注：

1. 业务逻辑是否正确
2. 是否符合项目需求
3. 代码是否容易理解和维护
4. 是否存在明显 Bug
5. 异常情况是否进行了合理处理
6. 是否存在安全问题
7. 是否影响项目其他功能
8. 是否存在不必要的代码
9. 项目代码风格是否保持统一
10. 是否误提交敏感信息

### Review 意见

Review 意见可以分为三类：

#### 必须修改

例如：

- 功能存在明显错误
- 存在安全问题
- 修改会导致其他功能无法正常使用

这类问题需要修改后才能合并。

#### 建议优化

例如：

- 代码可读性
- 命名方式
- 更合理的代码组织方式

这类意见可以根据实际情况进行讨论。

#### 疑问

Reviewer 对某个设计或实现方式存在疑问时，可以提出讨论。

---

## 六、收到 Review 后怎么办？

如果 Reviewer 提出了修改意见：

**不要关闭原来的 PR，也不要重新创建一个新的分支。**

直接在当前分支继续修改：

```text
修改
 ↓
commit
 ↓
push
 ↓
GitHub PR 自动更新
```

修改完成后，可以在 PR 中回复 Reviewer，说明哪些意见已经修改，哪些问题仍需要讨论。

---

## 七、代码合并

PR 经过 Review 并确认没有问题后，由项目维护者负责合并。

项目推荐使用：

**Squash and merge**

将一个开发分支中的多条 Commit 压缩为一个提交进入 `main`，保持 `main` 的提交历史简洁。

开发者一般不需要关心具体的合并操作。

你需要做的主要就是：

```text
写代码
 ↓
提交
 ↓
创建 PR
 ↓
根据 Review 修改
 ↓
等待合并
```

---

## 八、提交文件时请注意

严禁将以下内容提交到 GitHub：

```text
.env
API Key
Token
密码
数据库密码
个人隐私信息
本地敏感配置
```

如果项目需要环境变量，请使用类似：

```text
.env.example
```

提供配置格式，而不要提交真实配置。

例如：

```text
API_KEY=your_api_key_here
```

而不是：

```text
API_KEY=sk-xxxxxxxxxxxxxxxx
```

---

## 九、前端修改

如果修改了网站页面，建议在 PR 中附上截图。

例如：

```text
修改前：
[截图]

修改后：
[截图]
```

这样 Reviewer 可以更加直观地了解页面变化。

如果是 Bug 修复，也建议提供：

```text
问题表现
↓
修改方式
↓
修改后的效果
```

---

## 十、Issue

如果发现项目存在 Bug 或者有新的功能想法，可以先创建 Issue。

例如：

```text
标题：
移动端首页导航显示异常
```

内容可以包括：

```text
问题描述：
在手机端打开首页时，导航栏显示异常。

复现方式：
1. 打开首页
2. 使用手机尺寸访问
3. 查看顶部导航

期望结果：
导航栏正常显示。

实际结果：
部分内容发生遮挡。
```

如果某个 PR 解决了对应 Issue，可以在 PR 中关联：

```text
Closes #123
```

合并 PR 后，GitHub 会自动关闭对应 Issue。

---

## 十一、给第一次参与项目的同学

如果你以前没有使用过 Git / GitHub，不需要担心。

你可以先从简单任务开始，例如：

- 修改文字
- 修复页面样式
- 更新 README
- 添加社团资料
- 修复简单 Bug

遇到不清楚的地方，可以直接在 Issue 或 PR 中提出。

**这个项目本身也是一个学习 Git、GitHub、前端开发和团队协作的实践机会。**

---

## 十二、简单总结

如果你只记住下面这套流程就够了：

```text
① 拉取最新 main

        ↓

② 创建自己的分支

        ↓

③ 修改项目

        ↓

④ commit

        ↓

⑤ push

        ↓

⑥ 创建 PR → main

        ↓

⑦ 等待 Review

        ↓

⑧ 根据意见修改

        ↓

⑨ 等待维护者合并
```

**不要直接向 `main` 提交代码。**

一个分支尽量只完成一个任务。

有问题就提 Issue，有修改就提 PR。

欢迎大家一起维护这个项目。
